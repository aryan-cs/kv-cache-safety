suites:
  - system_leakage
  - refusal_safety
  - adversarial_refusal_safety
  - benign_overrefusal
  - instruction_following
  - capability_smoke
  - base_alignment_contrast
notes:
  - Built-in suites are diagnostic seed suites for code validation and smoke testing.
  - adversarial_refusal_safety places harmful requests after a long benign preamble for the registered adversarial placement condition.
  - base_alignment_contrast is scored with safe-minus-unsafe continuation log-likelihood margins for base-model H6 diagnostics.
  - Publication runs should ingest open public datasets with scripts/prepare_data.py --source hf.
