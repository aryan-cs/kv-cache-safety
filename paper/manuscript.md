# Cache-Mediated Safety Erasure Manuscript

The authoritative manuscript is the LaTeX source at `docs/latex/main.tex`.
This Markdown file is intentionally only a pointer so the repository does not
carry a second, stale paper draft with different wording or project-log details.

Do not submit the manuscript until
`scripts/report_publication_status.py --require-arxiv-bundle` reports that all
evidence gates pass. The current paper must remain framed as a registered
analysis protocol until primary results, causal results, declared audit-source
summaries, claim assessment, final figures, and the arXiv source bundle are
generated from real runs. Open-local-judge summaries must remain source-marked
and include calibration; they are not human audit summaries.
